今の環境：今の環境.mdを参照
## 各環境のpath記録
【surumePC】の作業path: /home/ryo52/workspace/exp-wp
【大学gpu】の作業path: /home/jovyan/work/srv21/workspace/le-wm
## 絶対に守ること
**docsやコードに「我々共通用語」を絶対に混ぜない。**
例えば、【surumePC】【大学gpu】などの表現をdocsやコードに混ぜると、論文提出時や他者にレビューをもらう際にノイズになる。結果再現性がとれず研究としての価値が大幅に低下する。これは、研究者としてあるまじき行為なので絶対に避ける。pathなど、必要な情報は許させる。しかし、先述のようなオリジナル用語や、我々のコンテキスト内でしか通用しない用語は絶対に避ける。ただし、新規手法の名称や、ノヴェルティのあるものなど、学術的に名称をつけるべき用語、つけないと冗長になる用語がある場合は、必ず、用語集new.mdに明記した上で使用することが可能である。
※ただし、許可された場所を除く（我々共通用語を含む環境差異の知見.md, 後述）

**間違いを発見したら必ず指摘すること**
後から気づくほど大変なことになるので、間違いは発見次第必ず報告や修正を行う。

## 今回の目的
今回の目的は、論文leworldmodelのtwo-room環境における再現実験を行い、dataとして残すことにある。
実験のスコープは、HFの事前学習済みcheckpointを入手し、planning実験を行い、論文通りの87％の数値が出ることを確かめるまでだ。
論文は、以下のファイルを参考にすること。paper.cleaned.mdは、論文を文字に起こしている。全読が必須である。しかし、画像やグラフなど、一部の要素は文字起こしされていないので、paper.pdfを参照する必要がある。特にグラフやアルゴリズムなどは再現実装の目標数値やコーディングなどで非常に重要なので必ず確かめる必要がある。
/home/ryo52/workspace/exp-wp/le-wm/papers/2603.19312_leworldmodel/paper.pdf
/home/ryo52/workspace/exp-wp/le-wm/papers/2603.19312_leworldmodel/paper.cleaned.md

two-room環境なので、two-room環境用のハイパラなどを正確かつ網羅的に設定する必要がある。まずは、論文やHF、そして、実際の公式リポジトリではどのような値であるかを精査して、一つずつ正確に再現するために設定していく必要がある。横着や推測で決めることは実験を破綻させてしまうことにつながるので絶対に禁止である。

## 環境について
コーディングする環境と、実際にgpuを使って訓練する環境は異なる。
- コーディングする環境 【surumePC】→デスクトップPCの5070ti(16GB*2)またはノートPCの5070(8GB)のいずれか1機が割り当てられる。
- 実際にgpuを使って訓練する環境【大学gpu】→3090, 4090, A100(40GB)のいずれか1機が割り当てられる。コーディングする環境でコーディングが終わり次第、訓練する環境にgithubを使って転送し、訓練を行う。
本プロンプトの冒頭に、今の環境がどちらの環境かを明示している。
環境を行き来するので、pathなどの環境によって変動する環境変数は変更しやすい位置にかつ、どのようなpathを設定すればいいかを明記して親切にコーディングを行う(ex: コメントなどで環境切り替え時のpathをすぐに切り替えれるようにするなど)
なお、本プロンプトの冒頭「各環境のpath記録」に、各環境の作業pathを明示している。

## githubの運用について
### ブランチ運用
デフォルトブランチはdevelopとし、作業開始日にdevelopからブランチを切る。`main` のマージは**別途フロー**で行うため、main 直接 push は禁止。developブランチがまだない場合は、mainから切って作成する。ブランチ名は、必ず作業開始日つきの、yymmdd-○○という形で、わかりやすい名称にする。作業途中のものはdocs記載のブランチから使用する。ゆえに、docsには何という名前のブランチを使用して作業を開始したか明記する必要がある。
### コミット運用
**原則: 1コミット = 1意図。** 1つのコミットに複数の意図を混ぜない。
**分割の考え方:**
- 各コミットが「何を、なぜ変えたか」を1文で説明できる粒度にする。
- 意図が同じなら複数ファイルを1コミットにまとめてよい。
- 意図が異なるなら同一ファイルでも別コミットに分ける。
- 「大量だから分割」ではなく「意図が異なるから分割」。

## docsの運用について
### リマインド
**docsやコードに「我々共通用語」を絶対に混ぜない。**
例えば、【surumePC】【大学gpu】などの表現をdocsやコードに混ぜると、論文提出時や他社にレビューをもらう際にノイズになる。結果再現性がとれず研究としての価値が大幅に低下する。これは、研究者としてあるまじき行為なので絶対に避ける。pathなど、必要な情報は許させる。しかし、先述のようなオリジナル用語や、我々のコンテキスト内でしか通用しない用語は絶対に避ける。ただし、新規手法の名称や、ノヴェルティのあるものなど、学術的に名称をつけるべき用語、つけないと冗長になる用語がある場合は、必ず、用語集new.mdに明記した上で使用することが可能である。
※ただし、許可された場所を除く（我々共通用語を含む環境差異の知見.md, 後述）
### docs規則
全作業や知見はdocsとして記録する。docsの明確な運用方法はまだ決まり切っていないので、探りながら決定化していく。
ひとまず、
- docs/ブランチ名 で作業ごとのdocsを独立化する。
- 作業ごとの**docsには、恣意や推測を書かない**。エビデンス付きの事実を書く。必ず再現性を持つように書く。学術的な体裁で書く。あなたの恣意や推測も大事だと思いますがdocsには不要です。docsは基本事実のみで記載する必要があります。**学術的根拠のない恣意や推測は研究的にマイナス評価になるので気を付けてください**。
- 「我々共通用語」にあてはまらない例外用語：path, yymmdd-実験名の通し名, 用語集new.md記載の用語 →例外用語を追加したい場合、必要なときのみ私に相談すること。
### docs一覧
- 以下のようなmd分けは少なくとも想定。必要に応じて追加する。私の許可がないと記載できないものが存在する。
-  実験はdocs横断して把握できる、yymmdd-実験名の通し名をつける。通し名はpathと同様、記録して良い「我々共通用語」であり、docsで横断して記録可能である。
実験通し名.md →yymmdd-実験名の通し名と、その関連ファイルや記載場所を記録する。
作業記録.md →作業の進捗を記録する
推測作業プラン.md →なんとなくの計画を記載する。
確定作業プラン.md →作業の計画を記録する。ただし、確定した計画だけを記載する。推測作業プラン.mdや、様々な知見を通して、決定した計画を記録する。**私が許可した場合のみの記載が許される**。
実験結果.md →実験結果を記載する。
論文のエビデンスベースの知見.md →
我々共通用語を含む環境差異の知見.md→ここには特別に「我々共通用語」を使用することができる。【surumePC】【大学gpu】などの「我々共通用語」関連の知見を記録することができる。
用語集new.md→新規手法の名称や、ノヴェルティのあるものなど、学術的に名称をつけるべき用語、つけないと冗長になる用語がある場合、ここに記載することで、「我々共通用語」として今後docsに書くことができるが、基本使わず、**私が許可した場合のみの記載が許される**。
用語集old.md→他人が書いた論文オリジナル用語や難解な知らなかった用語をここに記録して、あとから用語の定義を確認できるようにするためのmdファイルである。

## 環境構築について
基本的に、論文の環境構築方法を再現するまたは、uvを使って環境を構築する。
uvを使う場合、例えば下記コマンドを使用することになる。
- uv run ○○
- uv add ○○

