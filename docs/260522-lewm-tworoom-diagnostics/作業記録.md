# 260522-lewm-tworoom-diagnostics 作業記録

### 事実

- 2026-05-22 に公式リポジトリ `https://github.com/lucas-maes/le-wm.git` の `refs/heads/main` を確認した。実測コマンド: `git ls-remote https://github.com/lucas-maes/le-wm.git refs/heads/main`。出力 commit は `c8a44170b22dfe15b0b1924e2845da97af926c42`。
- 2026-05-22 に `git fetch https://github.com/lucas-maes/le-wm.git main:refs/remotes/upstream/main` を実行し、`upstream/main` を取得した。
- 2026-05-22 に `git branch -f main upstream/main` を実行し、ローカル `main` を `upstream/main` と同じ commit に更新した。
- 2026-05-22 に `git branch 260522-lewm-tworoom-diagnostics main` を実行し、`main` から `260522-lewm-tworoom-diagnostics` を作成した。
- 既存の作業ツリーには未コミット変更が存在したため、`git worktree add /tmp/le-wm-260522-lewm-tworoom-diagnostics 260522-lewm-tworoom-diagnostics` を実行し、新ブランチ用の作業ツリーを `/tmp/le-wm-260522-lewm-tworoom-diagnostics` に作成した。元の作業ツリーの未コミット変更は変更していない。
- `260522-lewm-tworoom-diagnostics` の HEAD は `c8a4417 Merge pull request #63 from lucas-maes/lancedb` である。実測コマンド: `git log --oneline --decorate -n 1`。

### 現在検討中の点

- 診断実験コードは未実装である。
- TwoRoom の `proprio` 列に含まれる各次元の意味は、dataset の実データまたは `stable_worldmodel` 側の環境定義で確認する必要がある。
- LeWM 以外の比較対象として PLDM、DINO-WM、Sub-JEPA、RC-aux、GC-IDM をどの粒度で再現・比較するかは未確定である。

### 採用方針

- この作業の記録先は `docs/260522-lewm-tworoom-diagnostics/` とする。
- docs は `事実`、`現在検討中の点`、`採用方針` の3区分で記録する。
- `a: 表現情報量`、`b: 潜在距離と到達可能性の対応`、`c: multi-step dynamics の精度`、`d: planner cost の整合性` を分けて記録する。
