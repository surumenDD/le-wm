# 実験通し名

### 事実

- このブランチ名は `260522-lewm-tworoom-diagnostics` である。実測コマンド: `git branch --show-current`。

### 現在検討中の点

- 実験コードと出力先は未確定である。

### 採用方針

- この診断研究の初期通し名は `260522-lewm-tworoom-diagnostics` とする。
- docs の記録先は `docs/260522-lewm-tworoom-diagnostics/` とする。
